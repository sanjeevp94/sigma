description = "Generate a compact, non-authoritative local fallback index from feature specifications."

# Source: architecture-guard

prompt = """
# Consolidate Specs Command

## Ponytail Core Contract

Before continuing, you **MUST** read and apply `.specify/extensions/architecture-guard/templates/ponytail_core.md` (or `.specify/extensions/architecture-guard/templates/ponytail_core.md` in the extension source checkout) as the authoritative shared contract. Phase instructions may narrow but not weaken its safety or verification floor.

## Purpose

Generate `specs/system_context.md` as a compact offline fallback for Budgeted Architecture Context Retrieval. This file supplements active feature artifacts; it is not a canonical specification and must never replace the active active specification, design, and task artifacts (the exact filenames depend on the active SDD tool), security constraints, or constitutions.

## Procedure

1. Resolve the configured `context.fallback_file`, defaulting to `specs/system_context.md`.
2. Scan `specs/**/spec.md` in normalized path order. Exclude the fallback file, generated artifacts, and directories explicitly identified as archived. Do not modify source specifications.
3. If no source specs exist, report that no fallback was generated and leave any existing fallback untouched unless the user explicitly authorizes replacing it.
4. Read each source only far enough to extract its title or one-line purpose, requirement and acceptance-criteria identifiers, shared invariants, cross-feature dependencies, and explicit conflicts. Acceptance-criteria bodies remain in the source spec.
5. Deduplicate only statements that are clearly identical. Keep similar statements separate. Never resolve conflicts or ambiguity silently.
6. Report the resolved output path and preview the complete generated artifact without writing it.
7. Request explicit user approval in a separate interaction before creating or replacing `system_context.md` at the resolved output path. On approval, write the complete artifact in one operation. Keep feature entries to one line plus identifiers and provenance so the fallback does not become another full specification.

## Required Output

```markdown
# Generated System Context

> Generated by Architecture Guard. This non-authoritative fallback supplements active feature artifacts and must not replace them.

## Generation Metadata
- Generated At: [ISO-8601 timestamp]
- Source Count: [count]

## Shared System Invariants
- [Concise invariant with source paths]

## Feature Index
- **[Feature]** — [One-line purpose]; IDs: [requirement/acceptance IDs]; Source: `specs/<feature>/spec.md`

## Cross-Feature Dependencies
- [Dependency with source paths]

## Unresolved Conflicts and Ambiguities
- [Distinct statements and their source paths; use `None identified` when empty]

## Source Manifest
- `specs/<feature>/spec.md`
```

## Completion Report

Report the output path, source count, conflicts found, excluded sources, and whether regeneration was complete. Do not claim exact token savings; those require representative benchmark runs.
"""